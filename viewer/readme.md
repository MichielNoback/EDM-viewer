# dependencies of the viewer

Run this to get up to speed with your dependencies:

```{r}
install.packages(c("igraph", "gridExtra", "plotly", "visNetwork", 
    "shinyWidgets", "dplyr", "tidyr", "readr", "here", "stringr",
    "ggplot2"))
```
